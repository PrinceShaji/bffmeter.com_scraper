Run makefile.py first. It creates the necessary databases (with headers) for scraping.Please note that this file overwrites any databases previously created.

Data is stored in csv format. To use it as csv, just change the file extension.